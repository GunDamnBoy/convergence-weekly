# 主題匯流訊號報 · 變更紀錄

這份是**歷史檔案**，給維護者回溯與比較用。
每週排程不讀這份（規格看 `AGENT_BRIEF.md`，維護流程看 `MAINTENANCE.md` 第 2 節）。

---

## 0. 怎麼用這份檔案

**要優化時**：先看第 2 節度量趨勢——哪一版真的降了成本、降在哪一塊，
再看第 4 節同類型改動的「被否決的選項」，避免重走已經走過的死路。

**要回溯時**：
1. 第 1 節找到版本與 commit 錨點
2. 第 4 節看該版的**逐檔改動**，決定要退哪幾個檔（多數版本是跨檔的，不能只退一個）
3. 看該版的**回溯要點**——有沒有資料格式相依（退了程式但 `data/` 已經有新格式的期別會炸）
4. 退完跑 `python3 healthcheck.py`；要驗單期檔就得**先重新備料**
   （`python3 prepare.py`），因為 `verify.py` 四個路徑參數一個都不能省——
   只給單期檔會回 `exit 2` 黃燈、逐字回查那批根本沒跑。
   再跟該版記錄的「驗證方式」對一次

> ⚠️ **commit 錨點的限制**：`com.kenny.dashpush` 每 180 秒自動 commit，訊息一律是
> `chore: update <時間>`，所以**一個版本不對應一個 commit**，而是對應「某個時間點的狀態」。
> 下表記的是**該版收工時的最後一筆 commit**，退到它等於退到那版完成的狀態。
> 不要期待 commit 訊息有語意。
> **同日連續發版時錨點會重合**（v0.7／v0.7.1 同為 `d14797b9`、v0.5.1／v0.6 同為 `ea0f8107`、
> v0.4／v0.4.1 同為 `c5952046`）——重合時**較早那一版無法用 commit 單獨還原**，
> 只能靠第 4 節的逐檔改動手動回退。

**要新增版本時**：第 1、2、4 節都要補（第 3 節的失效模式只在真的又踩到時才補）。
度量用 `python3 healthcheck.py --metrics` 一次印出來，不要用估的。

---

## 1. 版本索引

| 日期 | 版本 | 一句話 | 動到的檔案 | 類型 | 收工 commit |
|---|---|---|---|---|---|
| 2026-08-17 | **v1.0.1** | 以 writing-for-agents 六槓桿審 brief 與排程 prompt，先修 5 處會過期的抄本與指錯的指標 | `AGENT_BRIEF` `MAINTENANCE` `prepare.py` ＋排程 prompt | 修正 | `355e02e0`＋ |
| 2026-08-10 | **v1.0** | 深度總檢＋兩個新功能：發布閘門、全面數值對帳、XSS 跳脫、訊號帳本（記分板）、上游改版偵測、前端長期化 | ＋`publish.py` ＋`cwlib.py` ＋`.gitignore` `prepare.py` `verify.py` `make_index.py` `healthcheck.py` `index.html` `AGENT_BRIEF` `MAINTENANCE` | 架構＋功能＋安全 | `9f1ddbd5`→`5d9f41ae`＋ |
| 2026-08-09 | **v0.9** | 依第 002 期實跑的用量報告優化：骨架化、禁重複勞動、修 watch 綁定漏洞 | `prepare.py` `verify.py` `AGENT_BRIEF` `MAINTENANCE` `data/index.json` | 效能＋修正 | `7acb1d29`＋ |
| **v1.0** | 22,946 | 4,084 | **27,030** | 7 | 16 |
| 2026-08-08 | **v0.8** | 建立本檔：逐版詳述、度量趨勢、回溯要點、失效模式歸納 | ＋`CHANGELOG.md` `healthcheck.py` `AGENT_BRIEF` `MAINTENANCE` | 流程 | `46f4192b`＋ |
| 2026-08-08 | **v0.7.1** | 瘦身後複查，修 12 處含一個零新增判定 bug | `prepare.py` `make_index.py` `AGENT_BRIEF` `MAINTENANCE` | 修正 | `d14797b9` |
| 2026-08-08 | **v0.7** | token 優化：備料與 index 組裝腳本化、prompt 減半 | ＋`prepare.py` ＋`make_index.py` `AGENT_BRIEF` `MAINTENANCE` `healthcheck.py` | 效能 | `d14797b9` |
| 2026-08-06 | **v0.6** | 接入監控庫 `triggers`，背離節有現成裁判門檻 | `index.html` `verify.py` `healthcheck.py` `build_issue.py` `AGENT_BRIEF` `MAINTENANCE` | 功能 | `ea0f8107` |
| 2026-08-06 | **v0.5.1** | 補上 v0.5 整支漏掉的 `healthcheck.py`＋11 處文件不同步 | `healthcheck.py` `verify.py` `AGENT_BRIEF` `MAINTENANCE` | 修正 | `ea0f8107` |
| 2026-08-05 | **v0.5** | 接第四庫「每日五圖」＋監控庫 v1→v2 遷移 | `index.html` `verify.py` `build_issue.py` `data/index.json` `AGENT_BRIEF` `MAINTENANCE` | 功能＋破壞性 |`d77392f8`|
| 2026-08-03 | **v0.4.1** | 巡檢改動後複查，修 4 處（含 verify 假綠燈） | `verify.py` `AGENT_BRIEF` `MAINTENANCE` | 修正 | `c5952046` |
| 2026-08-03 | **v0.4** | 建站後首次巡檢，抓到 20 處不同步 | `index.html` `verify.py` `build_issue.py` `AGENT_BRIEF` `MAINTENANCE` ＋建立排程 | 修正 | `c5952046` |
| 2026-08-03 | **v0.3** | 改為 GitHub Pages 站台，歷史全保留、加跨期趨勢 | `index.html` `data/` `build_issue.py` `AGENT_BRIEF` | 架構 | — |
| 2026-08-03 | **v0.2** | 接入 AI 泡沫監控成為第三庫，新增「關鍵背離」節 | 原型階段 | 功能 | — |
| 2026-08-03 | **v0.1** | 雙庫原型，單一 HTML 交付 | 原型階段 | 起點 | — |

> `＋` 標記表示該版收工時尚有未推送的變更（dashpush 每 180 秒才推一次），
> 實際錨點是該 hash 之後的下一筆。
>
> v0.1–v0.2 是原型階段，當時還沒有本 repo 的檔案結構（v0.3 才建立），故無 commit 錨點。
> v0.1–v0.3 三版的詳述用「動機／改了什麼／實測結果」而非第 4 節的固定模板——
> 那三版是事後追記的，沒有當時的逐檔紀錄可還原，不硬套模板。

---

## 2. 度量趨勢（優化時的比較基準）

**每週固定成本**＝排程每次觸發必然消耗的量：讀 brief 全文 ＋ 讀 prompt 全文。
這是唯一值得長期追蹤的數字，因為它每週都會付一次。

| 版本 | brief 字數 | prompt 字數 | **每週必讀合計** | 腳本數 | `verify` 檢查批次 |
|---|---|---|---|---|---|
| v0.1–v0.2 | 未量測 | 未量測 | — | 0（原型） | — |
| v0.3 | 未量測 | 未量測 | — | 1 | — |
| v0.4 | 未量測 | 未量測 | — | 3 | 8 |
| v0.4.1 | 未量測 | 未量測 | — | 3 | 8 |
| v0.5 | 未量測 | 未量測 | — | 3 | 8 |
| v0.5.1 | 未量測 | 未量測 | — | 3 | 8 |
| v0.6 | 未量測 | 未量測 | — | 3 | 9 |
| **v0.7** | 21,741 → **18,721** | 7,330 → **2,985** | **21,706** | **5** | 9 |
| **v0.7.1** | **19,009** | **3,016** | **22,025** | 5 | 9 |
| **v0.8** | 19,278 | 3,016 | **22,294** | 5 | 9 |
| **v1.0.1** | 23,008 | 4,085 | **27,093** | 7 | 16 |

> ⚠️ 本表缺 v0.9 與 v1.0 兩列，且 v1.0 的度量那一列誤植在第 1 節的版本索引表中間。
> 已列入待修（本輪授權範圍不含它，刻意不順手改）。

**怎麼讀這張表**：

- **只有「字數」三欄是 v0.7 起才量測的**；腳本數與 `verify` 檢查批次是事後由檔案清單
  與程式碼回推的，可信。**從 v0.7 起每版都要填字數**，用 `healthcheck.py --metrics` 取數。
- `verify` 檢查批次 v0.5→v0.5.1 的 8→8 不是筆誤：v0.5.1 改的是**文件把數字寫錯**
  （「七件事」→八件事），實際檢查數沒變。
- **刻意不收「healthcheck PASS 數」**——它受執行環境影響（沙箱看不到 `~/.dashpush`、
  `errata` 條數、期數多寡都會改變總數），不是可跨版本比較的度量。
- v0.7 的 prompt 砍掉 59%，brief 只降 3,020 字（14%）。
  **brief 才是每週必讀的大宗**（19K vs 3K），下一輪優化的槓桿在這裡——
  **但砍的只能是非規則內容**（歷史、範例、重複敘述），
  規則本身砍不得，理由見 v0.7「被否決的選項」第一條。這是已知的死路，不要重走。
- v0.7.1 合計反而微升 319 字，因為修 bug 補了說明文字。
  **修正版通常會讓數字微升，這是正常的**——不要為了守數字而不寫清楚。
- v0.8 把變更紀錄整段移出 `MAINTENANCE.md`（18,086 → 13,786 字），
  但那**不計入每週成本**——維護文件排程不讀。列在這裡只是提醒別把它跟每週必讀搞混。
- v1.0 合計升到 27K——這一版是**用固定成本買保險**：新增的字全是規則
  （calls 登帳準則、publish 流程、上游改版分支），對應的是三條原本沒被守住的核心假設。
  與 v0.7 的教訓一致：省 token 的正道是砍回合數與重複勞動，不是砍規則。
- 表格沒有列「實際 token 用量」，因為那受當週資料量影響（投顧卡片數會變），
  不是穩定的比較基準。要比就比這裡的固定成本。

**還沒被量測、但可能是大頭的**：每週實際跑起來時，兩個子代理各讀一份摘要層
（實測 `adv.txt` 約 85K 字、`pod.txt` 約 30K 字——**均超出 brief 的目標值**，是來源庫擴編所致，規格允許超標並如實回報）——那是變動成本，
且是必要的（覆蓋率比細節重要）。若未來要動它，要先確認不是拿覆蓋率換 token。

---

## 3. 反覆出現的失效模式

這一節是本檔案對未來最有價值的部分：**同一類錯誤已經出現多次**。

### 3.1 「哪些檔案是一組」這份清單本身會過期（3 次）

| 何時發現 | 出事時清單寫什麼 | 實際漏了誰 | 後果 |
|---|---|---|---|
| v0.4 訂下、**v0.5 出事** | 三處：brief／`build_issue.py`／`index.html` | `verify.py` 不在清單 | v0.5 動 schema 時 `verify.py` 是臨時想到才補的 |
| **v0.5.1 發現** | 三處（同上） | `healthcheck.py` 整支漏掉 | 第 002 期一寫進去就會噴兩個 FAIL |
| **v0.7.1 發現** | 三處（brief／外殼／兩支檢查腳本） | v0.7 新增的 `prepare.py`／`make_index.py` | schema 一動它們會靜靜產出錯的東西 |

**現行對策**：清單**正本在 `AGENT_BRIEF.md` 第 3.0 節末**。
新增任何會讀寫單期 JSON 或 index 的檔案時，第一件事就是把它加進那份清單。

> ⚠️ 目前還有兩處複寫：`MAINTENANCE.md` 第 2 節第 4 條（與正本一致）、
> 第 6 節 v0.5.1 事故紀錄（**那是當時的版本，已標註為歷史敘述**）。
> 複寫本身就是 3.1 這個失效模式的溫床——下次動清單時順手把第 2 節那份改成引用。

### 3.2 規則寫在排程讀不到的地方（2 次）

排程每次觸發都是全新 session，**只讀 `AGENT_BRIEF.md` 與 prompt**。

- v0.4：「量化佐證不得取自 `events`」只活在 `MAINTENANCE.md` 與 `verify.py`——排程從來不知道這條。
- v0.4：樣本不足處置整條沒進 prompt，而當時投顧就是只留 3 天，等於每期都會踩到。

**現行對策**：`MAINTENANCE.md` 第 2 節末的自問——「這條規則排程執行時讀得到嗎？」

### 3.3 「改完再比對一次」每次都抓到東西（5 次，命中率 100%）

| 版本 | 複查抓到 | 其中會實際出錯的 |
|---|---|---|
| v0.4.1 | 4 處 | `verify.py` 缺參數時印「✅ 可以發布」（假綠燈） |
| v0.5.1 | 1 支腳本 ＋ 11 處文件 | `healthcheck.py` 硬驗六維（v2 期別必誤報） |
| v0.6 | 30 處 | `l1`–`l3` 不在合法欄位名清單（v2 期引用層分數會被誤判 FAIL）；`build_issue.py` 重建時洗掉 `errata` |
| v0.7.1 | 12 處 | `prepare.py` 零新增判定永遠不觸發（保護等於不存在） |
| v0.9 | 20 處 | 骨架的 `watch` 佔位字串會讓 `verify.py` 第 5.6 項**必定誤報**，且錯誤訊息把人導向查 trigger 表而不是填 watch |

**每一次都是在自己剛改完的東西裡抓到的。**

**現行對策**：維護 skill 把「再叫一次子代理獨立比對」列為固定步驟，不是可選項。
注意這些 bug 的共同特徵——**正常路徑測不到**，要嘛需要缺參數、要嘛需要特定資料狀態。

### 3.4 上游改版沒有通知機制（1 次，但影響最大）

監控庫 2026-08-04 從六維改三層，是第 001 期發布的隔天。
本系統完全不知情，直到 v0.5 對舊期重跑 `verify.py` 才因為崩潰而發現。
若沒發現，8/9 那期必然失敗。

**現行對策**：`verify.py` 的架構不符會給明確錯誤訊息，
`prepare.py` 的 `bub.txt` 也會把當前架構攤開來讓主線看到。
但**仍然沒有主動偵測上游改版的機制**——這是已知缺口，已列入 `AGENT_BRIEF.md`
第 6 節待決事項第 7 條。

---

## 4. 各版本詳述

### v1.0.1 — 2026-08-17 · 修正

**動機**　把 Matt Pocock 的 `writing-for-agents` 標準從維護 skill 往下延伸到**每天實際被執行的文件**
——`AGENT_BRIEF.md` 與排程 prompt。用六個槓桿（context pointer／資訊階層與 progressive disclosure／
完成條件／leading words／negation／pruning）審完，產出分三層的發現清單：
🟢 低風險高報酬 11 處、🟡 中風險高報酬 10 處、🔴 要先討論方案 5 處。
**本版只執行 🟢 層的前五項**——全部是「文件講的事實已經不成立」或「指標指到空地」，
不觸碰任何規則語意。其餘留待後續分批。

> brief 是排程每週完整讀一次的文件，改壞了就是下次沒產出，風險比改 skill 高得多。
> 所以刻意一輪只改一小批，且優先挑**刪掉會過期的抄本**這種低風險高報酬的項目。

**逐檔改動**

- `AGENT_BRIEF.md`
  - **§1 資料來源／§2 時間窗口**：「約 17 天 history」「約 17 個交易日」寫死的天數改為不綁數字
    （實際已長到 26 筆且會持續增長）。這是把 pruning 的 **cache** 判準套上去——
    會自己長大的東西不該在文件裡留一份抄本。
  - **§1.1 監控庫 v2 警語**：「`indicators` 的 id 在 v2 也全換了……舊期引用的 `hyoas`／`circular`
    等在新版不存在」是**事實錯誤**——兩者都還在 22 項裡，而 brief 自己在 §3.1 與 §5
    拿 `<code>hyoas</code>` 當 ✅ 範例。改為「有增有減、不是整組換掉，合法欄位名以當期
    監控庫實際的 id 為準」。同一份文件內自相矛盾，會讓合成者不敢用合法欄位。
  - **§2 樣本門檻**：刪掉「每日五圖 2026-08-05 才開始運作，**前幾期天數必然偏少**」
    ——第 003 期已是 7 天／35 張滿窗。保留條件式門檻（`<3 天` 那條規則本身未動）。
  - **§4 第 5 步**：登帳準則的指標 `§3.1` → `§3.2 末`。calls 區塊實際在 §3.2，
    §3.1 一個字都沒有——**指標指到空地**，排程照著查會找不到 schema。
  - **§6 待決第 5 條**：「7 條門檻目前全部 `state=0`，等第一條亮起來時檢視」已被事實推翻
    ——`gsy150` 自 2026-08-09（第 002 期）起 `state=1`，第 002、003 期 `trigLit` 皆為 1。
    改為「待檢視（條件已滿足）」。**這條的觸發條件早就滿足，卻因為文字寫死而永遠不會被觸發。**
- `MAINTENANCE.md`
  - §5 待辦同一句的第二份複本（`state=0`）同步更新。
  - §3 排程 prompt 正本：`第 3.1 節` → `第 3.2 節末`。
- `prepare.py`：兩處註解／骨架佔位說明的 `brief 3.1` → `brief 3.2 末`（L189、L262）。
  行為零改動，`py_compile` 通過。
- **排程 prompt**：同輪用 `update_scheduled_task` **整份取代**，只改第 55 行那一處指標；
  改完重讀 `SKILL.md` 確認 99 行與 YAML frontmatter 都在、其餘 93 行逐字未動。

**被否決的選項**

- **把 calls 區塊從 §3.2 搬進 §3.1**（讓內容去就指標，而不是指標去就內容）——否決。
  搬動等於重排兩個 schema 小節的邊界，是「一組要一起改」會牽動的那種改法；
  本輪的原則是不觸碰規則語意，改指標是一個字的事，搬區塊不是。列入後續待議。
- **順手修 CHANGELOG 第 2 節缺 v0.9／v1.0 兩列、以及 v1.0 度量誤植進第 1 節**——否決，
  不在本輪授權範圍。改為在第 2 節表下留一則 ⚠️ 註記，讓它下次一眼看得到。
- **順手改掉 `MAINTENANCE.md` §6 v0.5 事故紀錄裡同一句錯誤**（「`indicators` 21→22 項且 id 全換」）
  ——否決。§6 是**事故紀錄**，記的是當時查到什麼、怎麼判斷的；依「歷史不美化」原則不改寫，
  與第 001 期用 `errata` 而不改內文是同一條原則。**現行文件**（§4 已知的坑）那份已修。
- **同一輪把 🟡 層一起做掉**（exit code 語意、verify 5.6 盲區、feedback 靜默缺節等）——否決。
  brief 改壞的代價是下週沒產出，一輪一小批是刻意的節奏。

**驗證方式**

1. 改前先量測基準（brief 595 行／40,695B、MAINTENANCE 441 行／31,152B、prepare.py 432 行／24,674B），
   全部用精確錨點替換，改完逐檔驗總長：brief 595 行／40,797B（淨 ±0 行——A4 刪 1 行、A5 補 1 行）、
   MAINTENANCE 443 行（+2，A5 與 §4 已知的坑各補 1 行）、prepare.py 432 行／+7B（僅註解）。
   三檔行數變動與預期逐項對得上。度量：brief 22,946→23,008、prompt 4,084→4,085
   （`第 3.1 節`→`第 3.2 節末` 恰好 +1 字）、每週必讀合計 27,030→27,093。
2. **殘留掃描**：先掃字面（`17 天 history`／`約 17 個交易日`／`在新版不存在`／
   `前幾期天數必然偏少`／`見 §3.1`／`brief 3.1`／`第 3.1 節`／`目前全部`，四份維護檔零命中），
   再由子代理做**語義**比對——後者才抓到兩處同義複本：`MAINTENANCE.md` §4「`indicators` 的
   id 也全換」與 brief §6.6「前幾期樣本必然偏薄」，兩處已一併修正。
   > **教訓：字面掃描會給假的乾淨訊號。** A2／A4 這種「事實已過期」的修正，
   > 要找的是**同一個意思的每一份複本**，而複本很少用同一個字寫。
   > 這是失效模式 3.1（複本會過期）的又一次，只是這次複本是一句話而不是一份清單。
3. `python3 -m py_compile prepare.py` 通過。
4. `healthcheck.py`：**PASS 28／WARN 1／FAIL 0**，與改前完全相同，沒有新的 FAIL
   （WARN 是沙箱看不到 `~/.dashpush`，非故障）。
5. **排程 prompt 逐字回讀**：`update_scheduled_task` 之後重新 Read 實際生效的 `SKILL.md`，
   確認 99 行、frontmatter 完整、只有第 55 行與改前不同，且與 `MAINTENANCE.md` 第 3 節一致。
6. 子代理獨立複查本輪改動有沒有製造新漂移。

**回溯要點**　純文字與註解改動，**無資料格式相依**，退版不影響 `data/` 任何一期，
也不需要重新備料。要退的話四處一起退：brief 7 個 hunk、MAINTENANCE 3 個 hunk、`prepare.py` 兩處註解、
排程 prompt 一處（排程那份要用 `update_scheduled_task` 整份取代退回，不能局部編輯）。

> 附帶收穫：`MAINTENANCE.md` 第 2 節長年寫著「維護時讀不到實際生效的 prompt，
> 那是受保護目錄、Cowork 掛不進來，所以無法做逐字 diff」——**本輪實測讀得到**
> （`Read` 工具可以，只有沙箱 bash 掛不到），並實際完成了逐字 diff，結果與正本相符。
> 這條錯誤前提讓「逐字 diff」被當成不可能而放棄，防線只剩紀律。**列在 🟢 層待修（A6），
> 本輪未動**——它是維護文件的敘述，不影響排程執行，留給下一批一起改。

---

### v1.0 — 2026-08-10 · 架構＋功能＋安全

**動機**　使用者要求用最新能力做一次深度總檢並思考新功能。子代理以陌生工程師視角審查後，
發現**三條一直以為有保護、實際上沒有的核心假設**：
①「歷史永不改寫」只是 prompt 裡的一句話，且 dashpush 讓 verify 實際跑在發布之後；
② composite／twHeat／quadrant／zone 從來沒有被對過帳（verify 第 5 項只迴圈 dims）；
③ 逐字回查只驗最長片段（數字全在沒被驗的短片段裡）、三來源語料併成一坨（標錯 s 照樣過）。
另外外殼的 `esc()` 是死碼——上游字串一路 innerHTML 到底，零跳脫。

**逐檔改動**

- **新增 `publish.py`（發布閘門，唯一發布路徑）**：草稿 → 不可改寫守衛 → 組 index →
  折入 calls → verify 全過 → 原子寫入 data/（單期檔＋index＋calls＋upstream 指紋）。
  任何一步失敗 data/ 一個位元組都不動。exit 0/1/2/3 各有語意。
- **新增 `cwlib.py`（共用函式正本）**：`baseline`（同架構最早一筆，曾寫四遍）、
  `dim_ids`／`schema_ver`（「是不是 v2」曾有五種寫法）、`is_lit`（state 真值統一）、
  `zone_label`、`upstream_fingerprint`／`diff_fingerprint`、`need`（可行動的缺欄位錯誤）、
  `atomic_write_json`。
- `verify.py`：**quant vs 監控庫逐欄對帳**（composite／twHeat／stage／quadrant／regime／
  zone 標籤）；**index 快照值層級對帳**；**逐來源回查**（corpora dict，標錯 s 會被指出
  「在別的來源找得到」）；**全片段回查**取代只驗最長段；**量化佐證數字 token 對帳**
  （每個數字必須存在於監控庫原始檔）；值域 0–100；佐證日期格式；重複佐證偵測；
  空節擋下；watch 過度標記；缺結構欄位 short-circuit。
- `prepare.py`：clone timeout 180s＋list args；目錄不存在（上游改結構）與窗口零檔分開報；
  `delta` 無基準時填 `None` 不再假裝 0.0；跨年日期標籤保留年份；index 空清單防護；
  **上游指紋 diff**（PREP.md 頂部 🛑 段）；**帳本攤開**（未結案帳目逼驗收）。
- `make_index.py`：找回 `is_newest`（重跑舊期不推進發布時間）；entry 以 merge 保留既有欄位；
  原子寫入。保留為維護用手動工具，正式流程走 publish.py。
- `index.html`：**`S()` 白名單跳脫**（全部欄位過濾，僅放行 `<b>/<code>/<br>`，實測
  script／img onerror 注入被擋、白名單保留）；期別按鈕改最近 12 顆＋下拉；
  **趨勢圖日期 x 軸**（跳週不再假裝時間均勻）＋缺期斷線＋40 點降採樣；
  triggers >8 條收折；快取分級（index 永遠最新、單期檔以 updated 當版本）；
  hashchange 監聽＋pushState；**「判斷紀錄」記分板**（讀 calls.json 顯示戰績與逐筆帳目）。
- `healthcheck.py`：idl 未定義防護；node 檢查改 tempfile＋全部 script 區塊；
  updated 檢查容忍跨午夜（+1 天）；calls.json／upstream.json 解析與統計；檔案清單補三檔。
- `.gitignore`：收掉 work/（曾有被 dashpush 整包 commit 的風險）。

**新功能一：訊號帳本（記分板）**
單期 JSON 新增選填 `calls{open[],close[]}`——可證偽判斷登帳（claim＋judge 必填，
judge 優先綁 trigger），publish 機械折入 `data/calls.json`（唯一性／引用存在性／
result 合法性驗證，錯了整期不發布）。PREP.md 每期攤開未結案帳目逼驗收。
站台「判斷紀錄」區塊顯示 N 命中 M 落空 K 未決與逐筆帳目。
**它回答使用者最根本的問題：這套系統的判斷值不值得信。**

**新功能二：上游改版偵測**
`cwlib.upstream_fingerprint` 取七項指紋（dims 鍵／**dimMeta 權重**／triggers id＋name 全文／
indicators id／tw items／stage checklist 全文／zones 門檻表），基準存 `data/upstream.json`
（publish 成功後更新）。prepare 對基準做**明文 diff**（不是只比 hash），變更列在 PREP.md
頂部的 🛑 段落，並要求寫進本期 gaps。權重是最無聲的改版——欄位全不變、composite 意義已變。

**被否決的選項**
- 「dashpush 加 .publish-lock 機制」——否決，那要改本機 launchd 腳本，跨系統耦合；
  改成「檢查前不落地」在本 repo 內就能守住同一件事。
- 「數字 token 對帳做成敘事側也驗」——否決，敘事側數字格式變體太多（％／成／億），
  誤報率會高到每週都要人工排除；先只對量化側（格式可控）。

**驗證方式**　publish 三道閘負面測試（verify 擋下 exit 1／竄改守衛 exit 2／calls 折帳錯誤）；
XSS 注入實測（script 與 img onerror 被跳脫、b/code 白名單保留）；趨勢圖斷線與 12+下拉
Node 模擬渲染；兩期歷史檔對帳全綠；healthcheck PASS 26／FAIL 0；子代理獨立複查。

**回溯要點**　⚠️ 這一版把發布流程換掉了：退掉 publish.py 必須同時把 prompt 第 4 步退回
v0.9 的 make_index＋verify 流程，否則排程會呼叫不存在的檔案。`calls.json`／`upstream.json`
退掉後站台記分板自動隱藏（fetch 失敗靜靜略過），無資料相依。cwlib.py 被四支腳本 import，
不可單獨退。

---

### v0.9 — 2026-08-09 · 效能＋修正

**動機**　第 002 期是第一次由排程完整跑完並發布。**內容品質是成功的**——標題句是判斷不是主題、
上期 watch 被逐條驗收（甚至修正了上期自己的說法）、圖表側寫抓到圖表庫的自我矛盾、
三條共振全部真的有三個獨立聲音、量化佐證 8/8 全帶 `<code>`。
但用量報告顯示 **560 萬有效 token、77 回合，其中 51.7% 是每回合重讀的固定成本**，
而交叉檢查 JSON 又發現一個真的漏洞。

**逐檔改動**

- `prepare.py`
  - **新增 `--emit-skeleton`**：產出 `work/skeleton.json`——`date`／`issue`／`label`／
    `range`／`coverage` 與 `quant` 的**數值欄位**（`composite`／`zone`／`stage.current`／
    `twHeat`／`quadrant`／三層與變動／`triggers` 全帶）全部自動填好。
    `dims[].note`／`stage.delta`／`callout`／`quant.note` 仍標「（填：…）」——那四欄是判斷。
  - **`PREP.md` 內嵌量化底盤全文**（composite／象限／階段／台股熱度／三層變動／觸發器表），
    主線不必再另外讀 `bub.txt`。
  - 修 `range.quant` 會拖出時間戳（`meta.built[5:]` 少了 `[:10]`）、
    `range.narrative` 只取投顧未含節目與圖表、`coverage` 掉了卡片／集數／張數、
    `zone` 用 `lo/hi` 取值但 `zones` 實際是 `max` 門檻表（58.1 被判成「冷靜期」）、
    `schemaVer` 寫死 v2 改為讀 `meta.version`。
- `verify.py`
  - **新增第 5.6 項：`watch[]` 的 trigger 綁定檢查。**
  - `evidence[].s` 合法值改為有自己的輸出行（原本只進 `fails`，畫面上看不到）。
  - 缺 `--bub` 的 skipped 標籤補上被跳過的觸發器對帳與 watch 綁定兩批。
  - trigger id 比對加 `(?<![a-z0-9_])` 邊界，避免日後新增短 id 互吃。
- `AGENT_BRIEF.md` / `MAINTENANCE.md` / 排程 prompt：第 1 步加 `--emit-skeleton`；
  **明令不要讀腳本原始碼、不要自己逐條回查引文**；第 4／5 步改用骨架；
  子代理加「一次讀完不要分段」；`watch` 的 trigger id 規則；
  「一組要一起改」的清單把 `prepare.py` 的依賴範圍寫準（它現在硬依賴整份 schema）。
- `data/index.json`：第 002 期加 3 條 `errata`（見下）。

**修掉的真漏洞**

第 002 期 7 條 `watch` 裡**有 4 條的 trigger 綁定是壞的**：
`<code>ccc</code>`（那是 indicator id，trigger 是 `ccc12`）、
講 `hy80` 卻標成 `<code>stage</code>`、`y10_5` 與 `cpi4` 只寫文字沒標。
`verify.py` 當時擋不到——它只驗「id 存在於監控庫」，而 `ccc`／`stage` 都是合法欄位名。

**這正好打掉 v0.6 接 triggers 的全部意義**（「下期驗收直接查 `state` 翻轉」）。
新的第 5.6 項對第 002 期實跑，精準抓出那 4 條、與人工核對完全一致。
依「既有單期檔永不改寫」，內文不動，改為加 `errata`。

**被否決的選項**

- 「讓骨架把 `quant` 整區含判斷欄位也填掉」——否決。`dims[].note`／`stage.delta`／
  `callout`／`quant.note` 是判斷不是抄寫，自動填等於產出佔位垃圾。
- 「砍摘要層大小來省 token」——否決，理由同 v0.7：覆蓋率比細節重要。
  省的應該是**回合數**（51.7% 的成本乘數），不是資料量。

**驗證方式**　骨架實跑並逐欄檢查；`verify.py` 第 5.6 項做正反測試
（第 002 期抓 4 條、修正版放行、骨架佔位不誤報）；`healthcheck` PASS 23 / FAIL 0；
子代理獨立複查抓到 20 處（含**骨架的 `watch` 佔位字串會讓 5.6 必定誤報**這個自造陷阱），全數修正。

**回溯要點**　`prepare.py` 與 `verify.py` 的改動**無資料格式相依**，可單獨退。
但退掉第 5.6 項等於把 trigger 綁定的自動驗收保護拿掉；
退掉 `--emit-skeleton` 要同時把 prompt 第 4 步退回「自己寫 quant」。

---

### v0.8 — 2026-08-08 · 流程

**動機**　使用者要求「詳細記錄每一版本的更動，方便未來優化時當作比較參考，也方便回溯」。
原本的變更紀錄是 `MAINTENANCE.md` 裡的一張表，每版一格塞滿所有事——
可以讀，但**不能拿來比較，也不能拿來回溯**：沒有度量、沒有逐檔清單、
沒有 commit 錨點、沒有「退了會怎樣」。

**逐檔改動**

- **新增 `CHANGELOG.md`**（本檔），五節：
  - 第 0 節 使用說明，含**回溯 SOP 四步**與 commit 錨點的限制說明
  - 第 1 節 版本索引（一行一版：一句話／動到的檔案／類型／收工 commit）
  - 第 2 節 **度量趨勢表**——優化時的比較基準，追蹤「每週必讀合計」
  - 第 3 節 **反覆出現的失效模式**（清單過期 3 次、規則寫錯地方 2 次、
    改完複查命中率 4/4、上游改版無通知 1 次）
  - 第 4 節 各版逐一詳述，固定模板：動機／逐檔改動／被否決的選項／驗證方式／回溯要點
    （v0.1–v0.3 是事後追記，沒有當時的逐檔紀錄可還原，用簡化格式並已註明）
- `healthcheck.py`：新增 `--metrics` 旗標，印出 brief 字數、prompt 字數、
  **每週必讀合計**、MAINTENANCE 字數、腳本數、`verify` 檢查批次、當前 HEAD，
  並附註「PASS 數不在這裡且不該拿來跨版比較」。必要檔案清單補 `CHANGELOG.md`。
- `MAINTENANCE.md`：第 7 節改為指向 `CHANGELOG.md`；
  第 2 節流程第 3 條改為「四項都要寫＋跑 `--metrics` 取數」。
- `AGENT_BRIEF.md`：檔案樹補 `CHANGELOG.md`；**第 6 節的轉址句改指 `CHANGELOG.md`**
  （原本仍寫「已移至 `MAINTENANCE.md` 第 7 節」，那是 v0.7 的中繼狀態）；
  待決事項補第 7 條「上游改版沒有主動偵測機制」。

**設計取捨**

- **為什麼獨立成檔而不是留在 `MAINTENANCE.md`**：`MAINTENANCE.md` 是「怎麼維護」，
  歷史是另一種東西；而且它已經 18K 字，再長下去維護者自己會不想讀。
- **為什麼度量只追「每週必讀合計」**：實際 token 用量受當週資料量影響
  （投顧卡片數會變），不是穩定的比較基準。固定成本才可比。
- **v0.1–v0.6 的度量誠實留白**，沒有回填估計值。
  估的數字會被當成真的拿去比較，那比沒有數字更糟。

**被否決的選項**

- 「用 git commit 訊息當版本錨點」——否決。`dashpush` 每 180 秒自動 commit，
  訊息一律 `chore: update <時間>`，沒有語意。改為記錄**該版收工時的最後一筆 commit**，
  並在第 0 節明講這個限制。
- 「回填 v0.1–v0.6 的度量」——否決，理由同上。

**驗證方式**　`healthcheck --metrics` 實跑並用它的輸出回填第 2 節（**初稿曾憑印象寫成
PASS 23、brief 19,077，實測是 22 與 19,060，已更正**——這份檔案的重點就是不估）；
`healthcheck` PASS 22 / FAIL 0；`verify.py` 對第 001 期通過；子代理獨立複查文件交叉引用。

**回溯要點**　純文件與檢查腳本，**無資料格式相依**，可安全單獨退。
退掉 `--metrics` 只會少一個取數捷徑。

---

### v0.7.1 — 2026-08-08 · 修正

**動機**　v0.7 瘦身後依規矩叫子代理獨立複查，抓到 12 處，其中一個是真 bug。

**逐檔改動**

- `prepare.py`
  - **零新增判定 bug**：`latest` 字典在某庫窗口內無檔時填「（窗口內無檔）」，
    而 `"（窗口內無檔）" > "2026-08-03"` 在 Python 字串比較裡是 `True`
    （`（` U+FF08 > `2`），導致 `fresh` 恆非空、**`exit 3` 永遠不會觸發**。
    改為先用 `re.match(r"20\d\d-\d\d-\d\d$")` 過濾非日期值。
  - 監控庫新舊判定由 `max(history[].date)` 改為 `meta.built`——
    `history` 盤後會落後頂層一格，用它判斷會誤判成沒新資料。
  - 既有 clone 由「不動」改為 `git pull --ff-only`——殘留上週的 `work/` 會靜靜拿舊資料備料。
  - 時區由 `dt.date.today()` 改為 `Asia/Taipei`（原本與 `make_index.py` 兩種基準）。
  - docstring 三處數值與程式碼不符（40–60K／12–20K／截斷三段）已對齊。
- `make_index.py`：docstring 的步驟編號中性化（原寫「第 5 步後半」，與 prompt 的第 4 步對不上）。
- `AGENT_BRIEF.md`：「一組要一起改」清單納入兩支新腳本並集中到 3.0 節末；
  「檢查八件事」→九件事（v0.6 加了觸發器對帳沒跟上）；§6 標題殘留「與變更紀錄」；
  「表態不寫值得持續觀察」補進第 5 節（原本只在 prompt）；
  摘要層規格補寫 `bullets` fallback、`events` 前 40 則、`tw`／`stage` 的截斷。
- `MAINTENANCE.md`：§2 清單同步；§4 過期的四處清單；待辦「八格」→九格；
  prompt 補「單邊訊號只標記不判斷」（原本只在 brief）。

**被否決的選項**　無（全部是修正，沒有設計取捨）。

**驗證方式**　偽造 `prev.date = 2099-01-01` 讓四庫都不比它新 → 確認 `exit 3`；
正常情境確認 `exit 0`；`healthcheck` PASS 22 / FAIL 0。

**回溯要點**　純程式與文件修正，**無資料格式相依**，可單獨退。
但退掉 `prepare.py` 那三處等於把零新增保護、上游新舊判定、殘留 clone 三個洞放回去。

---

### v0.7 — 2026-08-08 · 效能

**動機**　每週固定成本的三大塊：排程每週自己重寫約百行摘要 Python（含除錯迴圈）、
讀已長到 500 行的 brief（其中變更紀錄 3.5K 字對產出零貢獻）、prompt 與 brief 大量重複。

**逐檔改動**

- **新增 `prepare.py`**（備料一鍵化）：clone 四庫 → 產 `adv/pod/bub/cotd.txt` 四份摘要層
  → 印 `PREP.md`（涵蓋統計、各庫最新日期、**上一期 watch 全文**、**triggers 狀態表**、
  樣本偏薄旗標、零新增提示）。`exit 3` ＝ 零新增資料，依 §2.1 不產期。
  摘要層目標字數改為**自動收縮**（adv 截斷 110→80→60→45、pod summary 420→300→220、
  cotd reading 全文→300→200），因為來源庫已擴編（投顧 26 家、7 天達 866 卡）。
- **新增 `make_index.py`**（index 快照組裝）：從單期 JSON 自動組 `quantVer`／`quadrant`／
  `trigLit`／發布時間，保留 `errata`。取代手工編 JSON。
- `AGENT_BRIEF.md`：第 4 節第 1–2 步改為指向 `prepare.py`（原規格保留為腳本的實作規格）；
  第 5 步改為指向 `make_index.py`；檔案樹補兩支腳本；**變更紀錄表移出**。
- `MAINTENANCE.md`：接收變更紀錄表為第 7 節；排程 prompt 由 155 行 7,330 字
  重寫為 72 行 2,985 字。
- `healthcheck.py`：必要檔案清單補兩支腳本。

**prompt 的壓縮原則**（這是可複製的方法）

| 內容類型 | 處置 | 理由 |
|---|---|---|
| 機械環節（clone、壓縮、組 index） | 指向腳本 | 每週重寫就每週有機會寫錯 |
| `verify.py` 會硬擋的規則 | 壓縮成一行清單 | 丟了會被擋下重跑，不會靜靜錯掉 |
| 機器驗不了的判斷原則 | **全文保留** | 丟了沒有任何機制會發現 |

**被否決的選項**

- 「把 brief 也大幅砍掉」——否決。brief 是規格的唯一真相來源，
  砍它等於把規則丟進沒人看得到的地方（見失效模式 3.2）。
  正確做法是把**歷史**移走（已做），不是把**規則**移走。
- 「摘要層改小以省 token」——否決。規格明訂覆蓋率比細節重要，
  減卡片則數會直接傷害共振判定的樣本厚度。改成自動收縮**截斷長度**而非則數。

**驗證方式**　`prepare.py` 對真實四庫實跑（7 天／866 卡／40 集／20 張圖）；
`make_index.py` 對合成 v2 期實跑並確認 `errata` 保留；全流程串接後 `verify.py` 正確攔截；
15 條關鍵規則清單核對 prompt 全數在場；`healthcheck` PASS 22 / FAIL 0。

**回溯要點**　退掉兩支腳本要**同時把 prompt 退回 v0.6 版**，
否則排程會去跑不存在的檔案。brief 的第 4 節也要一起退（它現在指向腳本）。
**無資料格式相依**——兩支腳本產出的 `index.json` 與手工組的格式相同。

---

### v0.6 — 2026-08-06 · 功能

**動機**　背離節的「裁判方法」原本每期臨場定義門檻，跨期不一致也無法自動驗收。
監控庫 v2 的 `triggers` 是現成的客觀答案，而且正好補上第 001 期 `watch` 點名的缺口——
那條「10 年期美債升破 5%」當時在監控庫沒有對應欄位、無法程式化驗收，v2 的 `y10_5` 就是它。
**回饋迴圈閉合了，這是接它的主要理由。**

**逐檔改動**

- `AGENT_BRIEF.md`：`quant.triggers` 入 schema（7 條全帶）；`index.json` 加 `trigLit`；
  背離節規則改為「優先引用現成門檻」，`watch[]` 能對應的要寫 trigger id；
  跨期趨勢由八格變九格。
- `index.html`：量化底盤新增觸發器清單（標已觸發數）；趨勢圖加「觸發器已亮」格。
- `verify.py`：v2 必填 `triggers[]`；新增觸發器對帳（id 與 state 對監控庫、`trigLit` 對得上）。
- `healthcheck.py`：同步；另加 `errata` 型別檢查。
- `build_issue.py`：修 `--force` 重建舊期時會把 `index.json` 的 `updated` 往回推。
- `MAINTENANCE.md`：排程 prompt 同步（triggers 規則、trigLit）；
  §5 待辦結案「投顧保留天數」——覆核已是 6 天而非 3 天，
  「每期都會踩到樣本偏薄」那條敘述整個過期。

**被否決的選項**　「只帶已觸發的那幾條」——否決。沒亮的條目本身就是資訊
（「這些事都還沒發生」是一個判斷，不是空白）。

**驗證方式**　合成 v2＋triggers 期別跑 `verify.py` 全綠；
負面測試（故意寫錯 `trigLit`）確認被擋；模擬三期確認趨勢圖長出九格。

**回溯要點**　**有資料格式相依**：退掉後 `verify.py` 不再要求 `triggers`，
但已寫入的期別仍有該欄位（無害，外殼會忽略）。
`index.json` 的 `trigLit` 同理。趨勢圖會從九格退回八格。

---

### v0.5.1 — 2026-08-06 · 修正

**動機**　v0.5 收尾後叫子代理複查，抓到 **`healthcheck.py` 完全沒跟上 v0.5**。

**逐檔改動**

- `healthcheck.py`：硬驗六維的兩處改為依版本各驗各的；佐證來源標籤補「圖表」
  並由 warn 升為 FAIL；量化快照納入 `quantVer`／`quadrant`；`REQ` 補 `stamp`；
  新增 `sections` id 與順序檢查。
- `verify.py`：「七件事」→八件事（漏列 `evidence[].s` 檢查）；殘留的「六維變動」字樣。
- `AGENT_BRIEF.md` 與 `MAINTENANCE.md`：11 處文件不同步（詳見 v0.5.1 原始紀錄）。

**根因**　`healthcheck.py` 不在「三處一組」的清單裡——**而那份清單本身就是過期的**。
修的不只是腳本，是那份清單（見失效模式 3.1）。

**驗證方式**　對 v1 舊期與合成 v2 新期分別跑 healthcheck，兩者都要通過。

**回溯要點**　純檢查腳本收緊，退掉只會讓檢查變鬆，不會壞資料。

---

### v0.5 — 2026-08-05 · 功能＋破壞性

**動機**　使用者要求把每日圖表接進來。查證時發現兩件事，其中一件是急件。

**逐檔改動**

- **每日五圖入第四庫**，但明確定位為**非獨立來源**：它的 `about.upstream[0]` 就是
  投顧庫當日檔，計票時與投顧算同一票；角色是量化側第二裁判（數字自行重製）。
- **新增第四節 `charts`「圖表側寫」**，章節由六節變七節；
  外殼尾段編號改為依 `sections` 長度動態計算（4 節舊期與 5 節新期都不錯位）。
- **監控庫 v1→v2 遷移**：`quant` 新增 `schemaVer` 與 `quadrant`；
  跨期趨勢分「連續組」與「v2 組」，誠實標示斷點。
- `verify.py`：新增共振獨立性檢查（`VOICE` 表把投顧與圖表映到同一 key）；
  依版本驗維度；變動不跨改版相減。
- `AGENT_BRIEF.md`：新增 §2.1「零新增資料時不產期」；章節表擴為七節。
- `data/index.json`：新增選填 `errata`，用於標注發布後才發現的問題（外殼渲染黃色橫幅），
  並實際寫入第 001 期的 3 條勘誤。
- `build_issue.py`：**正式凍結**在第 001 期（v1 六維、4 節），docstring 加警語
  「不要照抄它的結構」；同時修 `--force` 重建時會洗掉 `errata`。
- `MAINTENANCE.md`：排程 prompt 整份重寫（加第四庫、v2 警告、計票規則）；
  §4 已知的坑補三條；§6 立案三則決策紀錄。

**被否決的選項**

- **把 D1–D6 映射到 L1–L3 讓歷史接得上**——否決。六維按主題分群、三層按資料更新頻率分群，
  硬接就是假趨勢，而跨期趨勢是本系統相對於「翻舊文章」的唯一差異。
  用假數字保住連續性，等於把唯一的差異親手做掉。
- **只保留三條連續線、從此不畫層次**——否決，資訊量損失太大，
  而且 `quadrant` 是 v2 最有價值的新東西。
- **派第三個子代理讀圖表庫**——否決。它的主題本來就來自投顧庫，
  獨立萃取只會複述投顧側已有的東西，平白多產出一份會製造假共振的材料。
- **改寫第 001 期那 5 條有問題的佐證**——否決。歷史全部保留的另一面是歷史不美化；
  改寫已發布的一期會讓所有前後對照失去意義。改為加 `errata`。
- **把那 5 條直接刪掉**——同樣否決。刪掉之後 `about.run` 的條數敘述又會對不上，
  等於用一個謊去補另一個謊。

**驗證方式**　合成 v2 5 節期別與 v1 4 節舊期分別通過 `verify.py`；
Node 模擬三期確認趨勢分兩組且標示斷點。

**回溯要點**　⚠️ **這一版有最強的資料格式相依**：
- 退掉 v2 遷移後，`verify.py` 會用六維驗證，但第 002 期以後的資料都是三層 → 會全數 FAIL。
- 退掉 `charts` 節後，5 節的期別會被 `CANON` 擋下。
- `errata` 退掉後外殼不再渲染勘誤橫幅，但資料還在（無害）。

**實務上這一版不可單獨回溯**——監控庫上游已經是 v2，退回六維等於接不上資料源。

---

### v0.4.1 — 2026-08-03 · 修正

**動機**　v0.4 巡檢改完後再叫子代理比對，在自己的改動裡又抓到 4 處。

**逐檔改動**

- `AGENT_BRIEF.md`：第 2 節「本期變動」寫成「history 第一筆對最後一筆」，
  但 `verify.py` 實算是「對頂層現值」——盤後 history 落後一格時照 brief 寫會被判 FAIL。
- `verify.py`：缺參數時會跳過整批檢查卻仍印「✅ 可以發布」（**假綠燈**），
  改為黃燈 `exit 2`。
- brief 與 prompt 兩份都補上「不要對本機跑 git 指令」（原本只在維護文件）。
- brief 內部兩處交叉引用指錯節次。

**驗證方式**　缺參數情境確認回 `exit 2` 並印黃燈。

**回溯要點**　純修正，無相依。退掉「假綠燈」那條會讓沒跑完的檢查看起來像通過。

---

### v0.4 — 2026-08-03 · 修正

**動機**　建站後第一次維護巡檢。用兩個獨立子代理比對 brief ↔ 排程 prompt ↔ 實作，
抓到 20 處不同步。

**逐檔改動（八項）**

1. 「四種訊號」與「六節章節結構」拆開，新增第 3.0 節正式定義章節；
   共識裂縫降級為 `tags` 標籤、台股節正式入規格。
2. 「量化佐證不得取自 `events`」入第 5 節——**原本只活在 `MAINTENANCE.md` 與 `verify.py`**，
   brief 與排程 prompt 兩邊都沒有，等於排程從來不知道這條。
3. 驗證步驟改為明確指向 `verify.py` 並修正 `--bub` 參數
   （原本兩份文件都誤寫成吃壓縮後的 `bub.txt`，實際吃原始 `data.json`）。
4. 發布與驗證順序對調，新增第 7 步含連不到本機的退路。
5. 新增「驗收上一期 `watch`」回圈。
6. `<code>` 慣例與 HTML 白名單入 schema。
7. 檔案樹補 `verify.py` / `healthcheck.py`。
8. 修正「不需要 launchd」的錯誤宣稱（`com.kenny.dashpush` 正是 launchd）。

另外兩處只記在 `MAINTENANCE.md` 第 6 節、當時沒進本表的：
**樣本不足處置整條沒進排程 prompt**（而當時投顧只留 3 天，等於每期都會踩到）；
`build_issue.py` 的 `about.run` 條數由寫死改為由結構實算，並加防覆寫閘門。

另：**建立排程任務**，並發現文件寫的 cron `30 13 * * 0`（UTC）是錯的——
排程工具吃**本地時間**，照抄會排到台北週一下午。改為 `30 21 * * 0`。

**驗證方式**　`healthcheck` 無新 FAIL；子代理獨立重比對。

**回溯要點**　純規格補正，未改變產出方法，無資料相依。

---

### v0.3 — 2026-08-03 · 架構

**動機**　使用者要求比照既有兩庫上網並保留歷史，便於前後對照觀察趨勢；
週日產出讓週一開盤前就有方向。

**改了什麼**　改為 GitHub Pages 站台（外殼 `index.html` ＋ 每期 JSON 全保留）；
執行時間定為每週日 21:30；`index.json` 加入每期量化快照以支援跨期趨勢圖。

**關鍵設計**　量化快照入 index，是為了讓「趨勢」是真的可畫，而不只是能翻舊文章。
**這一條後來成為整套系統最核心的約束**——v0.5 的斷點決策、v0.6 的 `trigLit`，
都是為了守住它。

**被否決的選項**　把跨期趨勢做成單一多線圖。否決理由：四條線量級接近、互相纏繞，
且需要一組通過色盲檢測的類別色，與「白底紅灰」的品牌配色衝突。
改成四格小圖（small multiples），各自縮放、單色紅線。

---

### v0.2 — 2026-08-03 · 功能

**動機**　前兩庫都是敘事，可能一起錯。

**改了什麼**　接入 AI 泡沫監控成為第三庫；新增「關鍵背離」章節。

**實測結果**　加入不看新聞的量化庫後，才出現本系統當時最有價值的一條訊號：
「敘事在講信用危機、但 HY OAS 三個月是收斂的」——**這種訊號雙庫版結構上不可能產生**。

---

### v0.1 — 2026-08-03 · 起點

**動機**　驗證跨庫比對能產生單庫看不到的訊號。

**改了什麼**　雙庫（投顧＋節目）原型，單一 HTML 交付。

**實測結果**　雙向落差都存在：pod 側的「Warsh 刻意要波動度」假說在 adv 側 0 次出現；
adv 側的記憶體成本傳導在 pod 側完全沒有主題。**這個結果決定了整套系統值得做下去。**
### v1.0 收尾複查（同輪，子代理獨立比對後的修正）

子代理複查命中率維持 6/6——這次抓到的最重的一條是 **brief 第 6 步整節仍是 v0.9
舊流程**（教執行者對 data/ 裡的檔案手跑 verify，前提「先落地再檢查」正是發布閘門要消滅的），
與同檔第 5 步直接矛盾。修正清單：

- **brief**：刪第 6 步舊流程（改寫為「驗證在 publish 內部發生」）、第 7 步改編號第 6 步、
  build_issue.py 背書改「僅供考古」、make_index.py 樹描述改「手動維護工具」、
  prepare.py 樹描述補 🛑 與帳本兩段、fold_calls 五項驗證寫全、void 註明僅人工、
  §5 加四條格式硬規則（M/D／佐證不重複／每節非空／值域 0–100——之前只有 verify 在擋，
  執行時讀得到的文件裡沒有，正是 3.5 節教訓的同型再現）、
  待決事項 7（上游偵測）劃掉並留設計筆記、「不要讀原始碼」清單補 publish/cwlib、
  交付訊息三行改四行（帳本戰績）
- **MAINTENANCE**：§2「一組要一起改」清單改為指向 brief 唯一正本（本檔複本已過期三次，
  複本本身就是失效模式 3.1）、§4 補三個 v1.0 新坑（publish 唯一路徑／落地非跨檔原子／
  void 僅人工）、§5 補兩個 v1.0 觀察項、§3 prompt 補格式硬規則與原始碼清單
- **程式**：cwlib 新增 `is_v2()`（publish 與 verify 原本各寫一份且 fallback 方向相反，
  漏寫 schemaVer 時錯誤訊息會把人帶偏）；publish exit code 對齊 docstring
  （草稿壞掉→3，之前 sys.exit(字串) 實際回 1）；落地改為**四份先全部序列化到 .tmp
  再連續 os.replace**——序列化失敗必在任何 replace 之前，把「部分落地」壓到磁碟層級
  才可能發生；prepare/verify docstring 跟上實作；骨架加 calls 空殼佔位
  （之前 prompt 要求填 calls 但骨架沒有佔位、brief 也沒點名——只照 brief 執行的話
  帳本永遠不會被登）
- **排程 prompt**：同步三處小改（原始碼清單、格式硬規則、與 MAINTENANCE §3 逐字一致）

留給未來的一句：**「唯一正本」宣稱要用 grep 驗證，不是用意志力維持**——cwlib 說
「是不是 v2 只在這裡」時，publish 和 verify 裡還各有一份。

追加（e2e 重跑抓到）：**冪等重跑被自己的帳本擋住**——同一草稿重發時，
`calls.open` 第二次折入被判「id 重複」而 exit 1，「補跑同一指令即可」的承諾不成立。
修法：同一期＋同日＋同 claim 的重複登帳、同一期＋同 result 的重複結案，
都視為無事可做而非錯誤。**冪等的單元不是「寫檔」而是「整條 publish」，
守衛加在哪一層，冪等就要驗到哪一層。**

